import React from 'react';

const Header = () => {

    return(
        <header className="header">
            <h1 className="title">
                Inventory App by Pius
            </h1>
            <h2 className="subtitle">
                Tool demonstrating Node, Express, SQL (SQLite + Sequelize), Jest, and React
            </h2>
        </header>
    );
}

export default Header;