import React from 'react';

const Operations = () => {

    return(
        <div>
            buttons to click:  (1) show all, (x) CRUD operations TBD
        </div>
    );
}

export default Operations